.tools-icons{
  display:grid;
  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
  gap:18px 16px;
  margin-top: 6px;
}

.tool-item{
  display:flex;
  align-items:center;
  gap:10px;
  font-size:12.5px;
  font-weight:600;
}

.tool-item img{
  width:28px;
  height:28px;
  object-fit:contain;
}
